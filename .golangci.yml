version: "2"
linters:
  default: none
  enable:
    - asciicheck
    - depguard
    - dogsled
    - errcheck
    - funlen
    - goconst
    - goprintffuncname
    - gosec
    - ineffassign
    - misspell
    - nakedret
    - nolintlint
    - rowserrcheck
    - staticcheck
    - unconvert
    - unparam
    - unused
  settings:
    depguard:
      rules:
        main:
          deny:
            - pkg: github.com/alecthomas/assert
              desc: not allowed
            - pkg: github.com/magiconair/properties/assert
              desc: not allowed
    dupl:
      threshold: 100
    exhaustive:
      default-signifies-exhaustive: false
    funlen:
      lines: 200
      statements: 150
    goconst:
      min-len: 3
      min-occurrences: 3
    gocritic:
      disabled-checks:
        - dupImport
        - ifElseChain
        - octalLiteral
        - whyNoLint
        - wrapperFunc
      enabled-tags:
        - diagnostic
        - experimental
        - opinionated
        - performance
        - style
    gocyclo:
      min-complexity: 15
    govet:
      enable-all: true
      settings:
        shadow:
          strict: true
    lll:
      line-length: 140
    misspell:
      locale: US
    nolintlint:
      require-explanation: false
      require-specific: false
      allow-unused: false
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - funlen
          - mnd
        path: _test\.go
      # Test tables are full of throwaway credentials and echo-back HTTP handlers,
      # which gosec reports as leaked secrets and XSS sinks.
      - linters:
          - gosec
        path: _test\.go
        text: G101|G705
      # Table-driven tests repeat fixture values (versions, URLs, testdata paths)
      # on purpose: naming each one hurts readability more than it helps.
      - linters:
          - goconst
        path: _test\.go
      # The deprecated field is set on purpose, next to its replacement, so that
      # older GitLab servers keep working.
      - linters:
          - staticcheck
        path: pkg/plugins/resources/gitlab/mergerequest/create\.go
        text: SA1019
      - linters:
          - testpackage
        path: _expansion_test\.go
      - linters:
          - gocritic
        text: 'unnecessaryDefer:'
    paths:
      - test/testdata_etc
      - internal/cache
      - internal/renameio
      - internal/robustio
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - test/testdata_etc
      - internal/cache
      - internal/renameio
      - internal/robustio
      - third_party$
      - builtin$
      - examples$
