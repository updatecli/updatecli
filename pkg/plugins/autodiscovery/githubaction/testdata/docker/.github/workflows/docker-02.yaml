name: CI
on:
  push:
    branches: [ main ]
jobs:
  updatecli:
    runs-on: ubuntu-latest
    steps:
      - name: My first step
        uses: docker://ghcr.io/updatecli/updatecli:v0.67.0

      - name: My first step
        uses: docker://ghcr.io/updatecli/updatecli:latest
