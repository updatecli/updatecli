{ 
    "$schema": "https://json-schema.org/draft/2020-12/schema" ,
    "$id": "/schema/resource/source/jenkins",
    "type": "object",
    "description": "Get latest Jenkins version",
    "required": [
        "title",
        "kind",
        "spec"
    ],
    "properties": {
        "title": {
            "description": "Jenkins source title",
            "type": "string",
            "example": "Get latest Jenkins version"
        },
        "kind": {
            "description": "Define source kind",
            "enum": ["jenkins"] 
        },
        "spec" : {
            "description": "Define Jenkins source parameters",
            "type": "object",
            "properties": {
                "release": {
                    "description": "Define Jenkins release type",
                    "enum": [
                        "stable",
                        "weekly"
                    ]
                },
                "version": {
                    "type": "string",
                    "description": "Specify Jenkins version which should exist"
                }
            } 
        }
    }
}